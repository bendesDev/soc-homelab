---
# Pacotes instalados via pacman (repositórios oficiais).
pacman_packages:
  - ansible
  - docker
  - docker-compose

# Pacotes instalados via AUR (precisa de um helper como yay/paru já presente no sistema).
aur_packages: []
