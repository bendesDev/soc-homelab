# Gerenciado pelo Ansible (roles/dashboard) — não editar à mão.
- resources:
    cpu: true
    memory: true
    disk: /mnt/storage
- datetime:
    text_size: xl
    format:
      timeZone: America/Sao_Paulo
      hour12: false
