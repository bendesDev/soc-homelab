# Gerenciado pelo Ansible (roles/dashboard) — não editar à mão.
# "language" não existe aqui de propósito: testado e essa imagem trava o
# idioma da UI em inglês no build (next-i18next.config.js hardcoded pra
# locales: ["en"]), settings.yaml não tem como contornar isso. Ver
# CHANGELOG.md.
title: Painel do gabriel
theme: dark
color: slate
headerStyle: boxed
layout:
  Mídia:
    style: row
    columns: 4
  SOC Lab:
    style: row
    columns: 4
